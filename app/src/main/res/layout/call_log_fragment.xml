<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/recycler_coordinator_app_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:elevation="0dp"
        app:expanded="false"
        app:layout_behavior="org.thoughtcrime.securesms.conversationlist.ConversationFilterBehavior">

        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:id="@+id/collapsing_toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_scrollFlags="scroll|enterAlwaysCollapsed|exitUntilCollapsed">

            <org.thoughtcrime.securesms.conversationlist.chatfilter.ConversationListFilterPullView
                android:id="@+id/pull_view"
                android:layout_width="match_parent"
                android:layout_height="130dp"
                android:background="?attr/colorSurface"
                app:layout_scrollInterpolator="@android:anim/linear_interpolator" />

        </com.google.android.material.appbar.CollapsingToolbarLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <!--
        LIGHT PHONE: the call log itself is Compose now. It draws its own empty state, so the
        LinearLayout of Material TextViews that used to sit over the list is gone with the
        RecyclerView.
    -->
    <org.thoughtcrime.securesms.calls.log.light.LightCallLogView
        android:id="@+id/light_call_log"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <org.thoughtcrime.securesms.components.menu.SignalBottomActionBar
        android:id="@+id/bottom_action_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:visibility="gone"
        tools:visibility="visible" />

    <!--
        LIGHT PHONE: the bottom action panel a long press on a call opens. Full-screen because the
        panel covers the app's bottom bar while it is open and a tap in the band above it dismisses.
        Last in the file and elevated so that it draws over the multi-select bottom action bar, which
        can be up at the same time.
    -->
    <org.thoughtcrime.securesms.light.LightActionPanelView
        android:id="@+id/light_action_panel"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:elevation="13dp"
        android:visibility="gone" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
