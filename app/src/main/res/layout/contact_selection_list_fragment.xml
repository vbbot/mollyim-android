<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:viewBindingIgnore="true"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipe_refresh"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/chipRecycler">

        <!--
            LIGHT PHONE: the picker's rows, drawn with the Light SDK's Compose components. Keeps the
            id the fragment and the SwipeRefreshLayout already know it by.
        -->
        <org.thoughtcrime.securesms.contacts.paged.light.LightContactSearchView
            android:id="@+id/recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <TextView
            android:id="@android:id/empty"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="15dp"
            android:gravity="center|center_vertical"
            android:text="@string/contact_selection_group_activity__finding_contacts"
            android:textSize="20sp" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/chipRecycler"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:minHeight="48sp"
        android:orientation="horizontal"
        android:paddingStart="@dimen/dsl_settings_gutter"
        android:paddingEnd="@dimen/dsl_settings_gutter"
        android:scrollbars="none"
        android:visibility="gone"
        android:background="?attr/colorSurfaceContainer"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintTop_toTopOf="parent"
        tools:listitem="@layout/contact_selection_list_chip"
        tools:visibility="visible" />

    <!--
        LIGHT PHONE: the bottom action panel a long press on a contact opens. Full-screen because the
        panel covers the bottom of the screen while it is open and a tap in the band above it
        dismisses. Last in the file and elevated so that it draws over the selection chip strip,
        which can be up at the same time.
    -->
    <org.thoughtcrime.securesms.light.LightActionPanelView
        android:id="@+id/light_action_panel"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:elevation="13dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>