<?xml version="1.0" encoding="utf-8"?>
<org.thoughtcrime.securesms.components.InputAwareConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false"
    app:animateKeyboardChanges="true">

    <include layout="@layout/system_ui_guidelines" />

    <ImageView
        android:id="@+id/conversation_wallpaper"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:importantForAccessibility="no"
        android:scaleType="centerCrop"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline" />

    <View
        android:id="@+id/conversation_wallpaper_dim"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/black"
        android:visibility="gone"
        tools:alpha="0.2f"
        tools:visibility="visible" />

    <FrameLayout
        android:id="@+id/conversation_video_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="@id/conversation_item_recycler_frame"
        app:layout_constraintEnd_toEndOf="@+id/conversation_item_recycler_frame"
        app:layout_constraintStart_toStartOf="@+id/conversation_item_recycler_frame"
        app:layout_constraintTop_toTopOf="@+id/conversation_item_recycler_frame" />

    <FrameLayout
        android:id="@+id/conversation_item_recycler_frame"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/conversation_bottom_panel_barrier"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline"
        app:layout_constraintTop_toTopOf="parent">

        <org.thoughtcrime.securesms.conversation.mutiselect.MultiselectRecyclerView
            android:id="@+id/conversation_item_recycler"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:overScrollMode="ifContentScrolls"
            android:scrollbars="vertical"
            android:splitMotionEvents="false"
            tools:itemCount="20"
            tools:listitem="@layout/conversation_item_sent_text_only" />

    </FrameLayout>

    <TextView
        android:id="@+id/scroll_date_header"
        style="@style/Signal.Text.BodySmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="@drawable/sticky_date_header_background"
        android:elevation="9dp"
        android:gravity="center"
        android:paddingStart="12dp"
        android:paddingTop="4dp"
        android:paddingEnd="12dp"
        android:paddingBottom="4dp"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/conversation_banner_frame"
        tools:text="March 1, 2015"
        tools:visibility="visible" />

    <View
        android:id="@+id/toolbar_background"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="@id/toolbar"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline"
        app:layout_constraintTop_toTopOf="parent" />

    <!--
        LIGHT PHONE: the visible top bar is @id/light_top_bar below. This toolbar is kept purely as
        the functional host that Compose cannot replace: it is the `MenuProvider` host, it owns the
        collapsible `SearchView` action view that "Search in conversation" expands into, its height
        is what the message list pads itself by, and its overflow button is the anchor the Light
        bar's ellipses opens the popup against. It draws nothing of its own — no navigation icon,
        no title, no action items (ConversationFragment forces every item into the overflow) — and
        what little it does lay out sits behind the opaque Light bar.
    -->
    <org.thoughtcrime.securesms.util.views.DarkOverflowToolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="@dimen/signal_m3_toolbar_height"
        android:background="@color/transparent"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:minHeight="@dimen/signal_m3_toolbar_height"
        android:theme="?attr/actionBarStyle"
        app:contentInsetStart="46dp"
        app:contentInsetStartWithNavigation="0dp"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline"
        app:layout_constraintTop_toTopOf="@id/status_bar_guideline" />

    <org.thoughtcrime.securesms.conversation.v2.light.LightConversationTopBarView
        android:id="@+id/light_top_bar"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="@id/toolbar"
        app:layout_constraintEnd_toEndOf="@id/toolbar"
        app:layout_constraintStart_toStartOf="@id/toolbar"
        app:layout_constraintTop_toTopOf="@id/toolbar" />

    <!--
        Ongoing group call. This used to sit inside the toolbar next to the title; the Light bar has
        no room for it and would occlude it there, so it hangs just below the bar instead. It is
        gone unless a call is actually in progress, and its elevation keeps it above the banners
        that share this strip.
    -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/conversation_group_call_join"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:elevation="10dp"
        android:fontFamily="sans-serif-medium"
        android:text="@string/ConversationActivity_join"
        android:textAllCaps="false"
        android:textColor="?attr/colorOnSurface"
        android:visibility="gone"
        app:backgroundTint="?attr/colorSurfaceContainerHighest"
        app:cornerRadius="@dimen/material_button_full_round_corner_radius"
        app:icon="@drawable/ic_video_solid_18"
        app:iconGravity="textStart"
        app:iconTint="?attr/colorOnSurface"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        tools:visibility="visible" />

    <org.thoughtcrime.securesms.components.compose.ActionModeTopBarView
        android:id="@+id/action_mode_top_bar"
        android:layout_width="0dp"
        android:layout_height="@dimen/signal_m3_toolbar_height"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintHeight_min="@dimen/signal_m3_toolbar_height"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline"
        app:layout_constraintTop_toTopOf="@id/status_bar_guideline" />

    <FrameLayout
        android:id="@+id/conversation_banner_frame"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:clipChildren="true"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline"
        app:layout_constraintTop_toBottomOf="@+id/toolbar">

        <org.thoughtcrime.securesms.conversation.v2.ConversationBannerView
            android:id="@+id/conversation_banner"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ViewStub
                android:id="@+id/pinned_message_stub"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:visibility="gone"
                android:inflatedId="@+id/pinned_message_compose_view"
                android:layout="@layout/pinned_message_stub" />

            <ViewStub
                android:id="@+id/voice_note_player_stub"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inflatedId="@+id/voice_note_player"
                android:layout="@layout/voice_note_player_stub" />

            <ViewStub
                android:id="@+id/unverified_banner_stub"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inflatedId="@+id/unverified_banner"
                android:layout="@layout/conversation_activity_unverified_banner_stub" />

            <ViewStub
                android:id="@+id/review_banner_stub"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inflatedId="@+id/review_banner"
                android:layout="@layout/review_banner_view" />

            <ViewStub
                android:id="@+id/banner_stub"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inflatedId="@+id/banner_compose_view"
                android:layout="@layout/conversation_list_banner_view" />

        </org.thoughtcrime.securesms.conversation.v2.ConversationBannerView>

    </FrameLayout>

    <org.thoughtcrime.securesms.components.ConversationScrollToView
        android:id="@+id/scroll_to_mention"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="12dp"
        android:visibility="invisible"
        app:cstv_scroll_button_src="@drawable/ic_at_20"
        app:layout_constraintBottom_toTopOf="@id/scroll_to_bottom"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_goneMarginBottom="20dp"
        tools:visibility="visible" />

    <org.thoughtcrime.securesms.components.ConversationScrollToView
        android:id="@+id/scroll_to_bottom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="16dp"
        android:visibility="invisible"
        app:cstv_scroll_button_src="@drawable/ic_chevron_down_20"
        app:layout_constraintBottom_toTopOf="@id/conversation_bottom_panel_barrier"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        tools:visibility="visible" />

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/mention_fragment_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/conversation_bottom_panel_barrier"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline"
        app:layout_constraintTop_toBottomOf="@id/toolbar" />

    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/conversation_bottom_panel_barrier"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:barrierDirection="top"
        app:constraint_referenced_ids="conversation_input_panel,light_bottom_bar,attachment_editor_stub,conversation_disabled_input,scheduled_messages_stub,scheduled_messages,conversation_search_bottom_bar" />

    <!--
        LIGHT PHONE: the bottom slot is held by @id/conversation_input_panel or by
        @id/light_bottom_bar, never both, so what stacks above it has to hang off whichever is
        showing. A constraint on the panel alone would put the staged-attachment editor (and the
        scheduled-messages bar chained above it) underneath the Light bar every time the panel is
        collapsed, which is almost always.
    -->
    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/conversation_input_slot_barrier"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:barrierDirection="top"
        app:constraint_referenced_ids="conversation_input_panel,light_bottom_bar" />

    <ViewStub
        android:id="@+id/attachment_editor_stub"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:inflatedId="@+id/attachment_editor_stub"
        android:layout="@layout/conversation_activity_attachment_editor_stub"
        app:layout_constraintBottom_toTopOf="@+id/conversation_input_slot_barrier"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline" />

    <ViewStub
        android:id="@+id/scheduled_messages_stub"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:inflatedId="@+id/scheduled_messages"
        android:layout="@layout/conversation_activity_scheduled_messages_stub"
        app:layout_constraintBottom_toTopOf="@+id/attachment_editor_stub"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline"
        app:layout_goneMarginBottom="0dp" />

    <!--
        LIGHT PHONE: collapsed from the start. The panel is expanded only for the composer, a
        recording or a voice-note draft (see ConversationFragment.updateLightInputChrome); starting
        VISIBLE would flash Signal's compose bubble, emoji toggle and send button on every thread
        open, before the first state pass got to it.
    -->
    <include
        android:id="@+id/conversation_input_panel"
        layout="@layout/conversation_input_panel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@+id/conversation_input_space_left"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline" />

    <!--
        LIGHT PHONE: the thread's visible bottom chrome — call, attach, compose. It stands in for
        @id/conversation_input_panel, which is collapsed out of the layout behind it and expanded
        again only for the composer, a recording or a voice-note draft; the two are never both
        showing. It also stands aside entirely for the three states that take this same slot and hide
        the input panel with it: a pending message request (@id/conversation_disabled_input), an open
        search (@id/conversation_search_bottom_bar) and multi-select
        (@id/conversation_bottom_action_bar). See ConversationFragment.updateLightInputChrome.
    -->
    <org.thoughtcrime.securesms.conversation.v2.light.LightConversationBottomBarView
        android:id="@+id/light_bottom_bar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toTopOf="@id/keyboard_guideline"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline" />

    <include
        android:id="@+id/conversation_search_bottom_bar"
        layout="@layout/conversation_search_nav"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@id/keyboard_guideline"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline" />

    <org.thoughtcrime.securesms.conversation.v2.DisabledInputView
        android:id="@+id/conversation_disabled_input"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toTopOf="@id/keyboard_guideline"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline" />

    <TextView
        android:id="@+id/conversation_release_notes_floating_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="24dp"
        android:layout_marginBottom="12dp"
        android:background="@drawable/release_notes_input_pill_background"
        android:elevation="6dp"
        android:paddingHorizontal="24dp"
        android:paddingVertical="10dp"
        android:text="@string/ConversationFragment_the_only_official_chat_from_signal"
        android:textAppearance="@style/Signal.Text.BodyMedium"
        android:textColor="?attr/signal_text_primary"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@id/keyboard_guideline"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline" />

    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/conversation_input_panel_barrier"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:barrierDirection="top"
        app:constraint_referenced_ids="emoji_search_container,keyboard_guideline,navigation_bar_guideline" />

    <TextView
        android:id="@+id/conversation_input_space_left"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="?android:windowBackground"
        android:paddingStart="5dp"
        android:paddingEnd="5dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/conversation_input_panel_barrier"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline"
        tools:text="160/160 (1)"
        tools:visibility="visible" />

    <View
        android:id="@+id/compose_divider"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:alpha="1"
        android:background="@drawable/compose_divider_background"
        android:visibility="invisible"
        app:layout_constraintBottom_toTopOf="@id/conversation_bottom_panel_barrier"
        tools:visibility="visible" />

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/input_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="@id/navigation_bar_guideline"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline"
        app:layout_constraintTop_toTopOf="@id/keyboard_guideline" />

    <!--
        LIGHT PHONE: custom inputs normally impersonate the remembered IME height in @id/input_container.
        The attachment chooser is a real full-screen surface instead: it covers the thread chrome but is
        deliberately absent from every input barrier, leaving the input panel, drafts, staged attachments,
        recorder and bottom bar laid out and alive behind it.
    -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/full_screen_input_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@color/black"
        android:elevation="14dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/navigation_bar_guideline"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline"
        app:layout_constraintTop_toTopOf="@id/status_bar_guideline" />

    <!--
        LIGHT PHONE: the full-screen composer, everything above the text entry. It stops at the top of
        @id/conversation_input_panel so that the real ComposeText — which never moves, so that
        @-mention autocomplete, styling, spoilers and paste-an-image keep working — sits immediately
        below it and grows upward into it as the message wraps.
    -->
    <org.thoughtcrime.securesms.conversation.v2.light.LightComposerView
        android:id="@+id/light_composer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:elevation="12dp"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@id/conversation_input_panel"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline"
        app:layout_constraintTop_toTopOf="@id/toolbar" />

    <View
        android:id="@+id/nav_bar"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="@id/navigation_bar_guideline"
        app:layout_constraintBottom_toBottomOf="parent" />

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/emoji_search_container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="@id/keyboard_guideline"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline" />

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/polls_create"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        app:layout_constraintBottom_toBottomOf="@id/keyboard_guideline"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline" />

    <!--
        LIGHT PHONE: the bottom action panel — the call menu, and the context window a long press on a
        message opens. Full-screen because the panel covers the bottom bar while it is open and a tap
        in the band above it dismisses. Elevated over the elevated views it would otherwise be drawn
        under — the scroll date header at 9dp and the ongoing-group-call button at 10dp. It can sit
        above the reaction scrubber below without consequence: nothing in this thread inflates that
        any more (see ConversationFragment.showLightMessageMenu), and the shade that went with it is
        never made visible.
    -->
    <org.thoughtcrime.securesms.light.LightActionPanelView
        android:id="@+id/light_action_panel"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:elevation="13dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@id/parent_start_guideline"
        app:layout_constraintTop_toTopOf="parent" />

    <FrameLayout
        android:id="@+id/reactions_shade"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/reactions_screen_light_shade_color"
        android:foreground="@color/reactions_screen_dark_shade_color"
        android:visibility="gone" />

    <org.thoughtcrime.securesms.components.menu.SignalBottomActionBar
        android:id="@+id/conversation_bottom_action_bar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:layout_marginBottom="16dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/keyboard_guideline"
        app:layout_constraintEnd_toEndOf="@+id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@+id/parent_start_guideline" />

    <ViewStub
        android:id="@+id/conversation_reaction_scrubber_stub"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:inflatedId="@+id/conversation_reaction_scrubber"
        android:layout="@layout/conversation_reaction_scrubber"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/parent_end_guideline"
        app:layout_constraintStart_toStartOf="@+id/parent_start_guideline"
        app:layout_constraintTop_toTopOf="@+id/status_bar_guideline" />

</org.thoughtcrime.securesms.components.InputAwareConstraintLayout>
